# Specify this gem's dependencies in sqs_queue.gemspec
source 'https://rubygems.org'

git_source(:github) do |repo_name|
  "https://github.com/#{repo_name}"
end

# Consume gems from sqs_queue.gemspec
gemspec
